#!/usr/bin/env bash
# script that configures a new Ubuntu machine to respect requirements
sudo apt-get -y install nginx
sudp ufw allow 'Nginx HTTP'
echo 'Holberton School' > index.html
mv index.html /var/www/html/
